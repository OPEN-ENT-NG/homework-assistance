import { flexStartBoxStyle } from "~/core/style/boxStyles";

export const timeSelectorWrapper = {
  ...flexStartBoxStyle,
  gap: "1rem",
};

export const SVGStyle = { color: "primary.main", fontSize: "2.4rem" };
